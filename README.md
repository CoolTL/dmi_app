# dmi_app

# Model
Our model can be found in the model.py file, it is responsible for retrieving data and storing it in a dataframe, it also cleans up the data to prepare it for viewing.

# View
The view is contained within the pyside6 GUI files.

# Controller
Our controller is the main.py file, which connects the Model and View together, along with providing the other logic and programming needed.
